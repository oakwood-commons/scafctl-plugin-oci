version: 2

project_name: scafctl-plugin-oci

builds:
  - id: plugin
    main: ./cmd/scafctl-plugin-oci/
    binary: scafctl-plugin-oci
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/oakwood-commons/scafctl-plugin-oci/internal/oci.version={{ .Version }}

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

changelog:
  use: github-native
